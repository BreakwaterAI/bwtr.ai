const esc = value => String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function resourceNav(active) {
  return `<nav class="wrap resource-nav" aria-label="Evaluation resources">${[['reader-pack','Reader pack'],['poc-planner','PoC planner']].map(([route,name])=>`<a href="/${route}/" ${active===route?'aria-current="page"':''}>${name}</a>`).join('')}</nav>`;
}
export function readerPage(readers,chapters) {
  const doc=(slug,title,description)=>{
    const d=readers.documents.find(d=>d.file===slug+'.pdf');
    if(!d)throw Error('Missing public PDF: '+slug);
    return `<a class="reader-file" href="/assets/reader-pack/${d.file}"><span><strong>${title}</strong><small>${description} · ${d.pages} ${d.pages===1?'page':'pages'} · PDF · ${Math.ceil(d.bytes/1024)} KB</small></span><b aria-hidden="true">↗</b></a>`;
  };
  const descriptions={Secure:'Review assets, connections and the evidence behind an exposure.',Assure:'Review software findings against the source and its dependencies.',SOAR:'Examine an investigation, its proposed action and approval boundary.'};
  return `${resourceNav('reader-pack')}<section class="wrap reader-hero"><div><p class="eyebrow">BREAKWATER / PRODUCT LIBRARY</p><h1>Find the detail your next decision needs.</h1><p class="lead">Get the product story in one page. Set the scope with an evaluation brief. Go deeper when your team is ready.</p><p class="plan-small">Public briefings · September 2026 · No sign-in for PDFs</p></div><aside class="reader-feature"><span class="eyebrow">START HERE</span><h2>One portfolio. Three perspectives.</h2><p>Understand where Secure, Assure and SOAR fit before you choose a product.</p>${doc('portfolio-overview','Read the portfolio overview','Products and their roles')}${doc('pilot-engagement-outline','Plan the pilot engagement','Scope and review gates')}</aside></section>
    <section class="wrap reader-products" id="products"><div class="reader-heading"><h2>Choose your product.</h2><p>Overview → Evaluation → Product guide</p></div><div class="reader-grid">${['Secure','Assure','SOAR'].map(name=>{const slug=name.toLowerCase();return `<article class="reader-product"><span class="eyebrow">BREAKWATER</span><h3>${name}</h3><p>${descriptions[name]}</p>${doc(slug+'-one-page','Product overview','The decision it supports')}${doc(slug+'-evaluation-brief','Evaluation brief','What to test and agree')}${doc(slug+'-product-brief','Product guide','Walk through the evidence')}<a class="reader-file" href="#chapters-${slug}"><span><strong>Full-book chapter outcomes</strong><small>Browse below · full book requires sign-in</small></span><b aria-hidden="true">↓</b></a></article>`;}).join('')}</div></section>
    <section class="wrap reader-chapters" id="chapter-outcomes"><div class="reader-heading"><h2>Go deeper, chapter by chapter.</h2><p>Find the outcome you need before opening the full book.</p></div>${chapters.map(product=>`<details id="chapters-${product.name.toLowerCase()}"><summary>${esc(product.name)}<small>${product.chapters.length} chapters</small></summary><p><a class="text-link" href="${esc(product.book_url)}" target="_blank" rel="noopener noreferrer">Open the full ${esc(product.name)} book ↗</a> <span class="plan-small">Sign-in required</span></p><div class="reader-chapter-list">${product.chapters.map(ch=>`<details><summary>${ch.number}. ${esc(ch.title)}</summary><p>${esc(ch.outcome)}</p><a class="text-link" href="${esc(product.book_url)}" target="_blank" rel="noopener noreferrer">Read in the ${esc(product.name)} book ↗</a><p class="plan-small">Chapter ${ch.number} · sign-in required</p></details>`).join('')}</div></details>`).join('')}<p class="plan-small">Product captures are recorded demonstrations. Evaluate the selected installation against your agreed scope.</p></section>
    <section class="wrap reader-next"><article><span class="eyebrow">SCOPE AN EVALUATION</span><h2>Turn the reading into a plan.</h2><p>Define the footprint, data boundaries and responsibilities for a proof of concept.</p><a class="text-link" href="/poc-planner/">Open the PoC planner →</a></article></section>
    <script src="/assets/site-ui/RESOURCE_HUB_SCRIPT" defer></script>`;
}
export function plannerPage(kind) {
  const poc=kind==='poc';
  return `${resourceNav(kind+'-planner')}<section class="wrap planner-intro"><div><p class="eyebrow">${poc?'EVALUATION DESIGN':'CRYPTOGRAPHIC READINESS'}</p><h1>${poc?'Build your PoC brief.':'Frame your PQC transition.'}</h1><p class="lead">${poc?'Ten decisions. A deployment sketch, sizing targets and a shared starting point.':'Ten decisions. A timing scenario, migration priorities and a roadmap to review.'}</p></div><p class="planner-privacy">No account, scan, upload or AI call. Answers stay in this page unless you choose to download or share them. Nothing is saved automatically. Ordinary website access logs still apply.</p></section><section class="wrap planner-shell"><div data-planner="${kind}"><p>Enable JavaScript to use the planner. You can still <a href="/reader-pack/">read the public briefs</a> or <a href="/#contact">contact us</a>.</p></div><p data-planner-status role="status" aria-live="polite"></p></section><section class="wrap planner-method"><h2>${poc?'A scope discussion, not a deployment commitment.':'An explicit scenario, not a quantum forecast.'}</h2><p>${poc?'Hardware sizing is a heuristic, not measured capacity. Supported collectors, integrations and operating modes need engineering review before installation.':'The timing comparison uses data lifetime, estimated migration duration and your assumed quantum horizon. Results are questionnaire-based priorities, not measured risk or compliance findings.'}</p></section>`;
}
